# Diagnósticos — Cisternas del Miño SL (simulación)

Estado del análisis (capa 2). En v1: el JSON de "Guardar estado" de la planilla, fechado `YYYY-MM-DD-estado.json`, uno por sesión de trabajo. El más reciente es el vigente; los anteriores son histórico.

En v2 esto migra a Postgres (ver brief-v2). El importador de v2 acepta estos JSON.
